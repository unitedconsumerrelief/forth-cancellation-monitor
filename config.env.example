# Slack Configuration
SLACK_WEBHOOK_URL=https://hooks.slack.com/services/YOUR/SLACK/WEBHOOK/URL
SLACK_CHANNEL=#your-channel-name
SLACK_USERNAME=Gmail Monitor

# Gmail Configuration
GMAIL_QUERY=to:your-email@domain.com (subject:"Cancellation" OR subject:"Cancel" OR subject:"cancelled" OR subject:"Payment Error" OR subject:"Client File Has Been Returned") after:2025/09/01

# Google Service Account Credentials (for Render deployment)
# Get these from your service account JSON key file
GOOGLE_SERVICE_ACCOUNT_EMAIL=your-service-account@your-project.iam.gserviceaccount.com
GOOGLE_DELEGATED_EMAIL=your-delegated-email@domain.com
GOOGLE_PRIVATE_KEY="-----BEGIN PRIVATE KEY-----\nYOUR_PRIVATE_KEY_HERE\n-----END PRIVATE KEY-----"
GOOGLE_PROJECT_ID=your-project-id
GOOGLE_PRIVATE_KEY_ID=your-private-key-id
GOOGLE_CLIENT_ID=your-client-id

# Polling Configuration
POLL_INTERVAL_SECONDS=60
TIMEZONE=America/New_York

# Optional Settings
RETURN_FULL_BODY=true
MODE=combined   # 'server' (Flask only), 'worker' (polling only), or 'combined' (both)

# Server Configuration (for Render deployment)
PORT=10000
